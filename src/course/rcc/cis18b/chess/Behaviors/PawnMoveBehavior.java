package course.rcc.cis18b.chess.Behaviors;

public class PawnMoveBehavior implements MoveBehavior {
    /**
     * Move the piece to a new location.
     *
     * @param row
     * @param column
     */
    @Override
    public void move(int row, int column) {

        //reassign the piece to a new location

        //tell the application to update the screen

    }
}
